// Para criar objetos, usamos {} chaves 
// Objetos se parecem com arrays, porém utilizam palavras sua posicões \\

var objetos =  {};    // Objeto vazio

var carro = {
    marca:"BYD",     //Usa o padão chave: valor
    modelo: "Seal"
}

// Para acessar ou alterar um valor, usamos . (ponto)
console.log ("Marca:" , carro.marca);

// Adicionar novos elementos no objeto só usar o ponto (.)
carro.ano = 2024;

console.log(carro);   // Mostra todo o objeto só usar o ponto (.)
// IMPORTANTE: imprimir o array ou objeto inteiro só funciona no console.
 